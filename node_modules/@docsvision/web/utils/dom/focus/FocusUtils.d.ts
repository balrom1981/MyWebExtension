/** Ищет в дереве элемент, который может получить фокус. */
export declare function findFocusableElement({ container, mode, selectorToFocus }: {
    container: HTMLElement | undefined;
    mode: 'first' | 'last';
    selectorToFocus?: string | null;
}): HTMLElement | undefined;
/** Ищет в дереве элементы, которые могут получить фокус. */
export declare function findAllFocusableElements(container: HTMLElement | undefined): HTMLElement[];
export declare function isFocusable(el: HTMLElement): boolean;
export declare function elementScrolledInView(elem: HTMLElement, container: HTMLElement, partial?: boolean): boolean;
