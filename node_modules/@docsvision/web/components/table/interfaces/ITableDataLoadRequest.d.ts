import { IPluggableEntity } from "@docsvision/web/core/extensibility/plugins";
export interface ITableDataLoadRequest extends IPluggableEntity {
    /** Перезагрузить данные таблицы с сервера. */
    refresh?: boolean;
    /** Обновить источник данных (базовый курсор). */
    refreshSource?: boolean;
    pageNumber?: number;
    pageSize?: number;
}
