import { IPluggableEntity } from "@docsvision/web/core/extensibility/plugins";
export declare enum ColumnType {
    /**
     * Int
     */
    Integer = 0,
    /**
     * String
     */
    String = 1,
    /**
     * Double
     */
    Float = 2,
    /**
     * Datetime
     */
    DateTime = 3,
    /**
     * Boolean
     */
    Bool = 5,
    /**
     * Decimal column type
     */
    Decimal = 6,
    /**
     * Unique id data type
     */
    UniqueId = 7
}
/** Значение видимости столбца, предоставляемое некоторым расширением. */
export interface ICurrentColumnVisibility {
    /** Идентификатор, обозначающий источник значения. */
    id: string;
    /** Видим ли столбец. */
    visible: boolean;
}
export interface IColumn extends IPluggableEntity {
    id: string;
    name: string;
    /** Флаг, обозначающий что столбец не может быть видимым ни при каких условиях. */
    hidden?: boolean;
    /**
     * Содержит флаги текущщий видимости столбца, в связи некоторыми условиями.
     * Столбец видим если все элементы данного массива содержат visible: true.
     */
    currentVisibility?: ICurrentColumnVisibility[];
    /** Служебный столбец, который не содержит данные. */
    system?: boolean;
    order?: number;
    type?: ColumnType;
}
