import { IPluggableEntity } from "@docsvision/web/core/extensibility/plugins";
import { ICell } from './ICell';
export interface IRow extends IPluggableEntity {
    /** Идентификатор строки в таблице (как правило ее индекс). */
    id: string;
    /** Уникальный идентификатор строки таблицы. */
    uniqueId?: string;
    /** Идентификатор сущности в системе (например, идентификатор карточки). */
    entityId: string;
    cells: ICell[];
}
