import { Store } from 'effector';
/** Сервис, реализующий доступ к скролл-контейнеру таблицы. */
export interface IScrollContainerService {
    readonly $scrollContainer: Store<HTMLElement>;
    readonly $relativeContainer: Store<HTMLElement>;
    getScrollTop(): number;
}
/** Сервис, реализующий доступ к скролл-контейнеру таблицы. */
export declare type $ScrollContainer = {
    scrollContainer: IScrollContainerService;
};
export declare const $ScrollContainer: string | ((model?: $ScrollContainer) => IScrollContainerService);
