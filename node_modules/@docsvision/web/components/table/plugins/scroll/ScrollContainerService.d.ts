import { IScrollContainerService } from '@docsvision/web/components/table/plugins/scroll/$ScrollContainer';
import { $TableDom } from '@docsvision/web/components/table/plugins/table-dom';
import { $Domain } from '@docsvision/web/core/state-management';
import { Event, Store } from 'effector';
import "./ScrollContainerService.css";
/** Сервис для доступа к скролл-контейнеру таблицы. */
export declare class ScrollContainerDataService implements IScrollContainerService {
    private services;
    private container;
    $scrollContainer: Store<HTMLElement>;
    $relativeContainer: Store<HTMLElement>;
    initScrollContainer: Event<HTMLElement>;
    setRelativeContainer: Event<HTMLElement>;
    constructor(services: $TableDom & $Domain);
    private findScrollContainer;
    private findRelativeContainer;
    getScrollTop(): number;
    scrollToTop(): void;
    scrollToDown(): void;
}
