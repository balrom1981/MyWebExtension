import { $Domain } from '@docsvision/web/core/state-management';
import { Event, Store } from 'effector';
import { $ScrollPosition, ScrollWindow } from '../scroll';
import { $TableData } from '../table-data';
import { IVirtualizationWindow, IVirtualizationService } from './$Virtualization';
export interface VirtualizationOptions {
    /** Колличество строк сверху и снизу видимой области, которые будут также смонтированы. */
    overscan?: number;
    /** Минимальное колличество строк, которое добавляется за одну транзакцию. */
    overscanSensitivity?: number;
}
export declare const VIRTUALIZATION_DEFAULT_OVERSCAN = 20;
/** Реализация {@see $Virtualization} */
export declare class VirtualizationService implements IVirtualizationService {
    private options;
    private services;
    $window: Store<IVirtualizationWindow>;
    setWindow: Event<IVirtualizationWindow>;
    constructor(options: VirtualizationOptions, services: $TableData & $Domain & $ScrollPosition);
    onScrollPositionChange: (scroll: ScrollWindow) => void;
}
