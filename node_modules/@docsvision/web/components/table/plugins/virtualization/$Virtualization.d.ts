import { Store } from 'effector';
export interface IVirtualizationWindow {
    from: number;
    to: number;
}
/** Интерфейс сервиса {@see $Virtualization}  */
export interface IVirtualizationService {
    /** Индекс первой строки таблицы, которая присутствуют в DOM. */
    readonly $window: Store<IVirtualizationWindow>;
}
/** Сервис, выполняющий виртуализацию строк таблицы (отображение в DOM только текущих видимых строк). */
export declare type $Virtualization = {
    virtualization: IVirtualizationService;
};
export declare const $Virtualization: string | ((model?: $Virtualization) => IVirtualizationService);
