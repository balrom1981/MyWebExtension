import { Store } from 'effector';
/**
 * Интерфейс сервиса {@see $LazyLoading}.
 */
export interface ILazyLoadingService {
    $loading: Store<boolean>;
    checkAllRowsLoaded(): boolean;
    loadAllRows(): void;
}
/** Динамически загружает строки таблицы. */
export declare type $LazyLoading = {
    lazyLoading: ILazyLoadingService;
};
export declare const $LazyLoading: string | ((model?: $LazyLoading) => ILazyLoadingService);
