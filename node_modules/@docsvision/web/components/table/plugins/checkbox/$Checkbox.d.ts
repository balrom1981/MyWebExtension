import { HeaderCellState } from "@docsvision/web/components/table/plugins/checkbox/HeaderCellState";
import { Store } from "effector";
import { IRow } from "../../interfaces";
/** Интефрейс сервиса {@see $Checkbox} */
export interface ICheckboxService {
    $checkboxCellsState: Store<IRow[]>;
    $checkboxHeaderCellState: Store<HeaderCellState>;
    onHeaderCellClick(checked: boolean): void;
    onCellClick(row: IRow): void;
    updateCellsState(checked: boolean): void;
    updateHeaderCellState(headerState: HeaderCellState): void;
    clearCellState(cardIds?: string[]): void;
}
