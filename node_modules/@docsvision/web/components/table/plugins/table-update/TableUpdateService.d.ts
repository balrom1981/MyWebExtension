import { ITableUpdateService } from './$TableUpdate';
export declare class TableUpdateService implements ITableUpdateService {
    private update;
    constructor(update: () => void);
    updateAll(): void;
}
