export interface ITableUpdateService {
    updateAll(): void;
}
/** Сервис, позволяющий инициировать перерисовку таблицы. */
export declare type $TableUpdate = {
    tableUpdate: ITableUpdateService;
};
export declare const $TableUpdate: string | ((model?: $TableUpdate) => ITableUpdateService);
