/// <reference types="react" />
import { HtmlProps } from '@docsvision/web/core/component/HtmlProps';
import { SortDirection } from '@docsvision/web/components/table/plugins/sort';
import './ColumnListItem.css';
export interface ColumnListItemProps extends HtmlProps.li {
    sortDirection: SortDirection;
    isActive?: boolean;
}
/** Стилевой элемент списка {@see ColumnListButton}. */
export declare const ColumnListItem: (props: ColumnListItemProps) => JSX.Element;
