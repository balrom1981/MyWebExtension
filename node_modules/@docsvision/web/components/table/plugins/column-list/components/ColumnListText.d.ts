/// <reference types="react" />
import { HtmlProps } from '@docsvision/web/core/component/HtmlProps';
import './ColumnListText.css';
import { SortDirection } from '@docsvision/web/components/table/plugins/sort';
export interface ColumnListTextProps extends HtmlProps.span {
    sortDirection: SortDirection;
}
/** Стилевой элемент строки элемента списка {@see ColumnListButton}. */
export declare const ColumnListText: (props: ColumnListTextProps) => JSX.Element;
