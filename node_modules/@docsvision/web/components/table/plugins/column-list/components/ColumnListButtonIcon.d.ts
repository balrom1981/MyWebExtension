/// <reference types="react" />
import { HtmlProps } from "@docsvision/web/core/component";
import './ColumnListButtonIcon.css';
export interface ColumnListIconButtonProps extends HtmlProps.div {
    disabled: boolean;
    tooltip: string;
}
/** Стилевая иконка кнопки для показа списка списка {@see ColumnListButton}. */
export declare const ColumnListButtonIcon: (props: ColumnListIconButtonProps) => JSX.Element;
