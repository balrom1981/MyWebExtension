import { IColumn } from '@docsvision/web/components/table/interfaces';
import { IColumnListService, IColumnListOptions } from '@docsvision/web/components/table/plugins/column-list';
import { $ListView } from '@docsvision/web/components/table/plugins/list-view';
import { $TableSortService } from '@docsvision/web/components/table/plugins/sort';
import { $TableData } from '@docsvision/web/components/table/plugins/table-data';
import { $Resources } from '@docsvision/web/core/localization/$Resources';
import { $Domain } from '@docsvision/web/core/state-management';
import { Store, Event } from 'effector';
export declare class ColumnListService implements IColumnListService {
    private services;
    static Relevance: string;
    $columns: Store<IColumn[]>;
    $options: Store<IColumnListOptions>;
    $relevance: Store<IColumn>;
    setColumns: Event<IColumn[]>;
    $relevanceIsActive: Store<boolean>;
    setRelevanceIsActive: Event<boolean>;
    constructor(options: IColumnListOptions, services: $Domain & $TableSortService & $TableData & $Resources & $ListView);
    sort(column: IColumn): Promise<void>;
}
