import { IColumn } from "@docsvision/web/components/table/interfaces";
import { Store } from "effector";
export interface IColumnListOptions {
    /** Разрешено ли показывать "Релевантность" */
    enableRelevance?: boolean;
}
export interface IColumnListService {
    readonly $columns: Store<IColumn[]>;
    readonly $options: Store<IColumnListOptions>;
    readonly $relevance: Store<IColumn>;
    readonly $relevanceIsActive: Store<boolean>;
    sort(col: IColumn): Promise<void>;
}
export declare type $ColumnList = {
    columnList: IColumnListService;
};
export declare const $ColumnList: string | ((model?: $ColumnList) => IColumnListService);
