import { ITableData } from "@docsvision/web/components/table/interfaces";
import { IRowAlternationService } from '@docsvision/web/components/table/plugins/row-alternation/$RowAlternation';
export declare class RowAlternationService implements IRowAlternationService {
    tableData: () => ITableData;
    constructor(tableData: () => ITableData);
    alternationEnabled: boolean;
    useAlternativeRowStyle(rowIndex: number): boolean;
}
