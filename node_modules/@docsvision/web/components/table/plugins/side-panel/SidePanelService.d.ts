import { $ResizeColumn } from "@docsvision/web/components/table/plugins/resize-column/$ResizeColumn";
import { $Domain } from "@docsvision/web/core/state-management";
import { $DeviceType } from "@docsvision/web/core/system-services/environment/DeviceTypeService";
import { Store, Event, Domain, Subscription } from "effector";
import { $TableData } from "../table-data";
import { ISidePanelService, SidePanelTab } from "./$SidePanel";
export declare const SIDE_PANEL_IDENT = 268;
export declare class SidePanelService implements ISidePanelService {
    private services;
    domain: Domain;
    tabAvailableChanged: Event<boolean[]>;
    tabAvailableChangedSub: Subscription;
    $isOpen: Store<boolean>;
    toggle: Event<boolean | void>;
    $tabs: Store<SidePanelTab[]>;
    $tabsLoading: Store<boolean>;
    $availableTabs: Store<SidePanelTab[]>;
    setTabsLoading: Event<boolean>;
    registerTab: Event<SidePanelTab>;
    unregisterTab: Event<string>;
    resetTabs: Event<void>;
    $hasAvailableTabs: Store<boolean>;
    $activeTab: Store<SidePanelTab>;
    openTab: Event<string>;
    $isMobile: Store<boolean>;
    constructor(services: $Domain & $ResizeColumn & $TableData & $DeviceType);
    onTogglePanel(open: boolean): void;
}
