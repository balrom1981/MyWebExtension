import { IRow } from '@docsvision/web/components/table/interfaces';
/** Интефрейс сервиса {@see $RowIndex} */
export interface IRowIndexService {
    getRowIndex(row: IRow): number;
}
/** Предоставляет информацию об индексе строки в общем массиве строк. */
export declare type $RowIndex = {
    rowIndex: IRowIndexService;
};
export declare const $RowIndex: string | ((model?: $RowIndex) => IRowIndexService);
