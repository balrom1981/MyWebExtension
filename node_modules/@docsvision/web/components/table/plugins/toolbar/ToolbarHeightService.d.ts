import { IToolbarHeightService } from "@docsvision/web/components/table/plugins/toolbar/$ToolbarHeight";
import { $Domain } from "@docsvision/web/core/state-management";
import { Store, Event, Domain } from "effector";
export declare class ToolbarHeightService implements IToolbarHeightService {
    private services;
    domain: Domain;
    $height: Store<number>;
    setHeight: Event<number>;
    constructor(services: $Domain, defaultHeight?: number);
}
