import { Store, Event } from 'effector';
export interface IToolbarHeightService {
    readonly $height: Store<number>;
    setHeight: Event<number>;
}
export declare type $ToolbarHeight = {
    toolbarHeight: IToolbarHeightService;
};
export declare const $ToolbarHeight: string | ((model?: $ToolbarHeight) => IToolbarHeightService);
