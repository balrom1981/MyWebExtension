import { IColumn } from "@docsvision/web/components/table/interfaces";
import { SortDirection } from '@docsvision/web/components/table/plugins/sort/SortDirection';
import { Event, Store } from "effector";
export interface ITableSortService {
    /** Ячейка шапки грида, на которую в данный момент навёлся пользователь */
    readonly $pointedToColumn: Store<IColumn>;
    setPointedToColumn: Event<IColumn>;
    processHeaderClick(column: IColumn): Promise<void>;
    getSortDirection(column: IColumn): SortDirection;
}
export declare type $TableSortService = {
    tableSortService: ITableSortService;
};
export declare const $TableSortService: string | ((model?: $TableSortService) => ITableSortService);
