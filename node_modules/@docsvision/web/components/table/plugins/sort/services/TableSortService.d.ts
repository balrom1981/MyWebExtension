import { IColumn } from '@docsvision/web/components/table/interfaces';
import { ITableSortService } from '@docsvision/web/components/table/plugins/sort/services/ITableSortService';
import { SortDirection } from '@docsvision/web/components/table/plugins/sort/SortDirection';
import { $Domain } from '@docsvision/web/core/state-management';
import { Store, Event } from 'effector';
export declare class TableSortService implements ITableSortService {
    private onSortRequested;
    private services;
    $pointedToColumn: Store<IColumn>;
    setPointedToColumn: Event<IColumn>;
    constructor(onSortRequested: (column: IColumn, sortDirection: SortDirection, services?: any) => Promise<void>, services: $Domain);
    processHeaderClick(column: IColumn): Promise<void>;
    getSortDirection(column: IColumn): SortDirection;
    setSortDirection(column: IColumn, direction: SortDirection): void;
}
