import { ITableDomService } from '@docsvision/web/components/table/plugins/table-dom/$TableDom';
import { $Domain } from '@docsvision/web/core/state-management/$Domain';
import { Event, Store } from 'effector';
/** Сервис для доступа к параметрам таблицы. */
export declare class TableDomService implements ITableDomService {
    private services;
    private tableChild;
    private table;
    private heightValue;
    $root: Store<HTMLElement>;
    setRoot: Event<HTMLElement>;
    $header: Store<HTMLElement>;
    setHeader: Event<HTMLElement>;
    constructor(services: $Domain);
    getTableHeight(): number;
    get header(): HTMLElement;
    set header(node: HTMLElement);
}
