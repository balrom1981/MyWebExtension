import { Event, Store } from 'effector';
/** Сервис, реализующий доступ к параметрам таблицы. */
export interface ITableDomService {
    readonly $root: Store<HTMLElement>;
    readonly setRoot: Event<HTMLElement>;
    readonly $header: Store<HTMLElement>;
    readonly setHeader: Event<HTMLElement>;
    getTableHeight(): number;
}
/** Сервис, реализующий доступ к параметрам таблицы. */
export declare type $TableDom = {
    tableDom: ITableDomService;
};
export declare const $TableDom: string | ((model?: $TableDom) => ITableDomService);
