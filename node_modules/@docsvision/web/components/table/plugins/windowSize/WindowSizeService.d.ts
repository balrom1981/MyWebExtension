import { IWindowSizeParams, IWindowSizeService } from "@docsvision/web/components/table/plugins/windowSize/$WindowSize";
import { $Domain } from "@docsvision/web/core/state-management";
import { Store, Event } from "effector";
export declare class WindowSizeService implements IWindowSizeService {
    private services;
    $size: Store<IWindowSizeParams>;
    setResize: Event<IWindowSizeParams>;
    constructor(services: $Domain);
    private browserChangedSize;
}
