import { Store } from 'effector';
export interface IListViewService {
    readonly $enabled: Store<boolean | null>;
    enable(enable: boolean): void;
}
export declare type $ListView = {
    listView: IListViewService;
};
export declare const $ListView: string | ((model?: $ListView) => IListViewService);
