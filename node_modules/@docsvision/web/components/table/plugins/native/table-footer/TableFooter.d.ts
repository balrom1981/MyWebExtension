import React from 'react';
import { HtmlProps } from '@docsvision/web/core/component';
export interface ITableFooterProps extends HtmlProps.tfoot {
    children?: React.ReactNode;
}
export declare const TableFooter: (props: ITableFooterProps) => JSX.Element;
