import { HtmlProps } from '@docsvision/web/core/component';
import React from 'react';
export interface ITableBodyProps extends HtmlProps.tbody {
    children?: React.ReactNode;
}
export declare const TableBody: (props: ITableBodyProps) => JSX.Element;
