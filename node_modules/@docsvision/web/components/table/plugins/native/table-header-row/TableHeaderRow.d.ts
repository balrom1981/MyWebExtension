import { HtmlProps } from '@docsvision/web/core/component';
import React from 'react';
export interface ITableHeaderRowProps extends HtmlProps.tr {
    children?: React.ReactNode;
}
export declare const TableHeaderRow: (props: ITableHeaderRowProps) => JSX.Element;
