import React from 'react';
import { HtmlProps } from '@docsvision/web/core/component';
export interface ITableHeaderProps extends HtmlProps.thead {
    children?: React.ReactNode;
}
export declare const TableHeader: (props: ITableHeaderProps) => JSX.Element;
