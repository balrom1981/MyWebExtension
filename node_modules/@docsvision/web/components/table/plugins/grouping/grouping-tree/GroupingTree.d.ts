/// <reference types="react" />
import { HtmlProps } from "@docsvision/web/core/component";
import { IGrouping } from "../interfaces/IGrouping";
import { IGroupingItem } from "../interfaces/IGroupingItem";
import { IGroupingPathItem } from "../interfaces/IGroupingPathItem";
import "./GroupingTree.css";
export interface IGroupingTreeProps extends HtmlProps.div {
    /** Дерево группировки. Значение null означает, что данные еще в процессе загрузки. */
    grouping: IGrouping | null;
    /** Структура группировки. */
    groupingPath: IGroupingPathItem[];
    /** Служебный пункт, обозначающий отображение всех карточек, без группировки. */
    everytingItem?: IGroupingItem;
    /** Выбранное значение группировки. */
    selectedItem?: IGroupingItem;
    /** Текст кнопки "показать еще" */
    showMoreText?: string;
    /** Текст сообщения о дозагрузке значений групп. */
    loadingItemsText?: string;
    /** Текст сообщения о загрузке данных дерева. */
    loadingText?: string;
    /** Текст элемента с пустым именем. */
    emptyNameText?: string;
    /** Величина отступа для уровня в пикселях. */
    levelIdent: number;
    onItemToggle: (item: IGroupingItem) => void;
    onItemSelect: (item: IGroupingItem) => void;
    onLoadMoreItems: (grouping: IGrouping, parentItem?: IGroupingItem) => void;
}
export declare const GroupingTree: (props: IGroupingTreeProps) => JSX.Element;
