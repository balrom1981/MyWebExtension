import { $Domain } from "@docsvision/web/core/state-management";
import dragula from "dragula-fixed";
import { Store, Event } from "effector";
import { IDragDetector, IDragContainer, IDragElement, IGroupingColumnDragService } from "./$GroupingColumnDrag";
import "dragula-fixed/dist/dragula.css";
import "./GroupingColumnDragService.css";
export declare class GroupingColumnDragService implements IGroupingColumnDragService {
    $containers: Store<IDragContainer[]>;
    $detectors: Store<IDragDetector[]>;
    $activeDrag: Store<IDragElement | null>;
    $isDragging: Store<boolean>;
    registerContainer: Event<IDragContainer>;
    registerDetector: Event<IDragDetector>;
    unregisterContainer: Event<string>;
    unregisterDetector: Event<string>;
    beginDrag: Event<IDragElement>;
    endDrag: Event<void>;
    startDrag: Event<void>;
    drakes: {
        [scope: string]: dragula.Drake;
    };
    constructor(services: $Domain);
    preventDefault(event: TouchEvent): void;
    getDrake(drakeScope: string): any;
    createDrake(): dragula.Drake;
}
