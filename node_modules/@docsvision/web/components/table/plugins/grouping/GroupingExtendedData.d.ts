import { ITableData } from "../../interfaces";
import { IGroupingItemInfo } from "./interfaces/IGroupingItemInfo";
import { IGroupingPathItem } from "./interfaces/IGroupingPathItem";
export interface IGroupingExtendedData {
    /** Идентификаторы столбцов, по которым возможна группировка. */
    columnsWithGroupingAvailable?: string[];
    /** Значение, по которому производится группировка. */
    selectedItem?: IGroupingItemInfo;
    /** Идентификаторы столбцов, составляющие текущую структуру группировки. */
    groupingPath?: IGroupingPathItem[];
    /** Скрывать ли столбцы при группировке по их значению. */
    hideColumnsOnGrouping?: boolean;
    /** Текст подтверждения, которое нужно показать при установке группировки. В случае отказа, отменять группировку. */
    groupingConfirmation?: string;
    /** Текст уведомления, которое нужно показать при установке группировки. */
    groupingNotification?: string;
    /** Изменение этого значения сигнализирует о необходимости перезагрузить дерево группировки. */
    groupingRefreshToken?: string;
}
export declare function getGroupingData(tableData: ITableData): IGroupingExtendedData;
