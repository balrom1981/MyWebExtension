import { IGrouping } from "./IGrouping";
import { IGroupingItemInfo } from "./IGroupingItemInfo";
export interface IGroupingItem {
    /** Информация о значении группировки. */
    info: IGroupingItemInfo;
    /** Значения группировки следующего уровня. */
    childGrouping?: IGrouping;
    /** Развернуты ли значения группировки следующего уровня */
    expanded?: boolean;
}
