import { IGroupingItem } from "./IGroupingItem";
export interface IGrouping {
    /** Значения групп */
    items: IGroupingItem[];
    /** Все ли значения групп загружены. */
    hasMoreItems?: boolean;
    /** Элементы в процессе загрузки. */
    loadingItems?: boolean;
}
