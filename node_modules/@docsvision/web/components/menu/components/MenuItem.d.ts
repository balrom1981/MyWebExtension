import { IComponentProps } from "@docsvision/web/core/component";
import React from "react";
/** @internal */
export interface IMenuItemProps extends IComponentProps {
    /** Tooltip of the menu item */
    title?: string;
    /** React.ReactNode that repersents menu item look */
    children?: React.ReactNode;
    /** Command action */
    onClick?(): void;
    /** Class "hide" will be added to command if visible = false */
    visible?: boolean;
    /** should add padding to child */
    padding?: boolean;
    /** Name for autotest purposes */
    name: string;
    dataTestId?: string;
    className?: string;
}
/**
 * @internal Представляет собой адаптивный элемент для меню {@link MenuBar}.
 * См. также {@link MenuContent}
 */
export declare const MenuItem: (props: IMenuItemProps) => JSX.Element;
