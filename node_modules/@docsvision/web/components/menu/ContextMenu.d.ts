import { PopoverMode } from "@docsvision/web/components/popover/Popover";
import { IComponentProps } from "@docsvision/web/core/component";
import React from "react";
/** @internal */
export interface IContextMenuProps extends IComponentProps {
    expanded: boolean;
    children?: React.ReactNode;
    mode?: PopoverMode;
    pageCoordinates?: any;
    isContextmenuEventNeed?: boolean;
    onClose?: () => void;
    targetGetter?: () => HTMLElement;
    handleOutsideClick?: boolean;
}
/** @internal */
export interface IAdaptiveMenuBarState {
    parentEl: HTMLElement;
}
export declare const ContextMenu: (props: IContextMenuProps) => JSX.Element;
