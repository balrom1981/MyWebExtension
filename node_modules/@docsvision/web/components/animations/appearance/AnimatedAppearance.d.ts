import React, { Component } from 'react';
export interface IAnimatedAppearanceProps {
    /**
     * Включена ли анимация. Если отключена, то содержимое появляется без анимации.
     */
    enabled?: boolean;
    /** Компонент, определяющий стили анимации. По умолчанию используется translateX -20%, opacity и transition 0.3s. */
    animation?: React.ComponentType<IAnimatedAppearanceAnimationProps>;
    /**
     * Время появления содержимого в милисекундах.
     */
    timeout?: number;
}
interface IAnimatedAppearanceState {
    shown?: boolean;
}
export interface IAnimatedAppearanceAnimationProps {
    shown: boolean;
}
/** Показывает содержимое с анимацией. */
export declare class AnimatedAppearance extends Component<IAnimatedAppearanceProps, IAnimatedAppearanceState> {
    state: IAnimatedAppearanceState;
    static defaultProps: IAnimatedAppearanceProps;
    componentDidMount(): void;
    private animation;
    render(): JSX.Element;
}
export {};
