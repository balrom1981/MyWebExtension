/// <reference types="react" />
import { HtmlProps } from '@docsvision/web/core/component/HtmlProps';
export interface IModalHostProps extends HtmlProps.div {
    name?: string;
    enabled?: boolean;
    services?: any;
}
/**
 * Предоставляет контейнер для отрисовки модального окна.
 * Позволяет отображать диалоговое окно вне react-компонента, в котором смонтирован.
 */
export declare const ModalHost: (props: IModalHostProps) => import("react").ReactPortal;
