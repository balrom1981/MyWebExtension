/// <reference types="react" />
import { HtmlProps } from '@docsvision/web/core/component';
import './ModalSidebarBox.css';
export interface IModalSidebarBoxProps extends HtmlProps.div {
    /** Флаг, открыта ли панель */
    isOpen?: boolean;
    /** Флаг, использовать ли ширину по умолчанию  */
    defaultWidth?: boolean;
    /** Ширина панели */
    width?: string;
}
/**
 * Представляет полотно боковой панели ({@see ModalSidebar).
 */
export declare const ModalSidebarBox: (props: IModalSidebarBoxProps) => JSX.Element;
