import { HtmlProps } from '@docsvision/web/core/component';
import './ModalDialogContainer.css';
import React from 'react';
export declare const MODAL_DIALOG_CONTAINER_CLASS = "dv-modal-dialog__container";
export interface IModalDialogContainerProps extends HtmlProps.div {
    /** Флаг открытия окна */
    isOpen?: boolean;
    /** Флаг поднятия события click внутри окна к родительскому элементу. По умолчанию: true */
    stopClickPropagation?: boolean;
    /** Временно отключает обработчик нажатия Esc. */
    disabledEscHandler?: boolean;
    /** Обработчик события нажатия Esc. */
    onEsc?: (ev: React.KeyboardEvent<any>) => void;
}
/**
 * Представляет контейнер для содержимого всплывающего окна.
 */
export declare const ModalDialogContainer: (props: IModalDialogContainerProps) => JSX.Element;
