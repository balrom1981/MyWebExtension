/// <reference types="react" />
import "./Label.css";
import "./LabelToggle.css";
import { HtmlProps } from '@docsvision/web/core/component';
export interface LabelProps extends HtmlProps.label {
    disabled?: boolean;
}
/** Метка, выполняющая роль контейнера для всех остальных элементов {@see SliderCheckBox}. */
export declare const Label: (props: LabelProps) => JSX.Element;
