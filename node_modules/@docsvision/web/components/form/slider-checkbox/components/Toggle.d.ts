/// <reference types="react" />
import "./Toggle.css";
import "./LabelToggle.css";
import { HtmlProps } from '@docsvision/web/core/component';
export interface ToggleProps extends HtmlProps.span {
    checked?: boolean;
    disabled?: boolean;
}
/** Переключатель - основной визуальный элемент {@see SliderCheckBox}. */
export declare const Toggle: (props: ToggleProps) => JSX.Element;
