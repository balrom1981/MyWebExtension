/// <reference types="react" />
import './ButtonStyledBody.css';
import { HtmlProps } from '@docsvision/web/core/component/HtmlProps';
/** Режимы выравнивания кнопки {@see Button} */
export declare enum ButtonAlignModes {
    Center = 0,
    Left = 1
}
/** Цветовые режимы кнопки {@see Button} */
export declare enum ButtonColorModes {
    Primary = 0,
    Secondary = 1
}
export interface ButtonStyledBodyProps extends HtmlProps.div {
    colorMode?: ButtonColorModes;
    align?: ButtonAlignModes;
    iconClass?: string;
    loading?: boolean;
    loadingIconClass?: string;
    loadingIconCenter?: boolean;
    value?: string;
    disabled?: boolean;
}
/** Стилевой контейнер кнопки {@see Button}. */
export declare const ButtonStyledBody: (props: ButtonStyledBodyProps) => JSX.Element;
