/// <reference types="react" />
import './Container.css';
import { HtmlProps } from '@docsvision/web/core/component/HtmlProps';
export interface ContainerProps extends HtmlProps.button {
}
/** Функциональный контейнер кнопки-иконки {@see IconButton}. */
export declare const Container: (props: ContainerProps) => JSX.Element;
