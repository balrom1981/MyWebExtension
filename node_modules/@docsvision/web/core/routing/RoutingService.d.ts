import { IApplication } from '@docsvision/web/core/routing';
import Navigo from 'navigo';
import { INavigateOptions, IRoutingService } from './IRoutingService';
import { RouteContext } from './RouteContext';
export declare class RoutingService implements IRoutingService {
    private readonly application?;
    private setContentComponentAction;
    private lastContentComponent;
    private navigo;
    private initialized;
    constructor(application?: IApplication);
    initializeRouter: () => void;
    isInitialized: () => boolean;
    getInternalRouterInstance: () => Navigo;
    navigateToRoute: (route: string, options?: INavigateOptions) => void;
    resolveRoute: () => void;
    goToRoute: (route: string, options?: INavigateOptions) => void;
    updateUrl(path: string, affectBrowserHistory?: boolean): void;
    generateNotFoundRoute: (handler?: () => void) => void;
    getLastRouteResolvedParams: () => RouteContext;
    registerRoutes: (mapper: any, processRoute: any) => void;
    private parseQueryParams;
}
