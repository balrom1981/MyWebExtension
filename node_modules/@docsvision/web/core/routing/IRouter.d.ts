import Navigo from "navigo";
export interface IRouter extends Navigo {
}
export declare type $Router = {
    router: IRouter;
};
export declare const $Router: string | ((model?: $Router) => IRouter);
