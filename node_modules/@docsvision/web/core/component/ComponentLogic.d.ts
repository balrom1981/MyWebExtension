export declare abstract class ComponentLogic<TOptions = {}> {
    protected _options: TOptions;
    get options(): TOptions;
    updateOptions(options: TOptions): void;
    abstract init(props: TOptions): void;
}
