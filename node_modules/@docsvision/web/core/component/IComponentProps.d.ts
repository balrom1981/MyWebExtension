import { ComponentLogic } from '@docsvision/web/core/component/ComponentLogic';
export interface IComponentProps<LogicT extends ComponentLogic<any> = any, ViewT = any> {
    onSetupLogic?: (logic: LogicT) => LogicT;
    onSetupView?: (view: ViewT) => ViewT;
}
