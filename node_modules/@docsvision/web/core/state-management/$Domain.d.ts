import { Domain } from "effector";
/** Сервис, задающий общий домен для сущностей effector, используемых в таблице. */
export declare type $Domain = {
    domain: Domain;
};
export declare const $Domain: string | ((model?: $Domain) => Domain);
