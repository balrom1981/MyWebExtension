import { StringMap } from '@docsvision/webclient/System/StringMap';
export interface IFileUploadOptions {
    fields?: StringMap;
    headers?: StringMap;
    onProgressChange?: (process: number) => void;
    url: string;
    fileField: string;
}
/** Сервис для загрузки файлов */
export interface IFileUploadService {
    upload<T>(file: File, options: IFileUploadOptions): Promise<T>;
}
export declare type $FileUpload = {
    fileUpload: IFileUploadService;
};
export declare const $FileUpload: string | ((model?: $FileUpload) => IFileUploadService);
