/** Сервис для передачи react элемента из ref. */
export interface IContentElementProviderService {
    setContentElement(element: HTMLElement): void;
    getContentElement(): HTMLElement;
}
/** Сервис для передачи react элемента из ref. */
export declare type $ContentElementProvider = {
    contentElementProvider: IContentElementProviderService;
};
export declare const $ContentElementProvider: string | ((model?: $ContentElementProvider) => IContentElementProviderService);
