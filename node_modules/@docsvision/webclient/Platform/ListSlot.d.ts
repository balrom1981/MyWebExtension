/// <reference types="react" />
import { HtmlProps } from '@docsvision/web/core/component';

export interface IListSlotProps extends HtmlProps.div {
    focused: boolean;
}
export declare function ListSlot(props: IListSlotProps): JSX.Element;
