/** Меняет значение с задержкой. Используется для показа анимации загрузки только если загрузка длиться больше чем {@see timeout}. */
export declare function useDelay(loading: boolean, timeout?: number): boolean;
