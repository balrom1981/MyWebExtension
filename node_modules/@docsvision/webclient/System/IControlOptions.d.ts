/** Сервис для переопределения реализации ControlTemplate */
export interface IControlOptions<TParams, TLogic, TView> {
    onSetupLogic?(logic: TLogic, params: TParams): TLogic;
    onSetupView?(view: TView, params: TParams): TView;
}
