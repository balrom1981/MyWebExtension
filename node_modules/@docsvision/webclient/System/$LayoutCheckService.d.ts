export interface ILayoutCheckService {
    /**
     * Проверяет, что карточка не заблокирована и не была изменена в другой сессии.
     * @return fail срабатывает, когда карточка заблокирована и есть изменения; иначе - срабатывает done.
     */
    checkLockAndModified(): Promise<void>;
}
export declare type $LayoutCheck = {
    layoutCheck: ILayoutCheckService;
};
export declare const $LayoutCheck: string | ((model?: $LayoutCheck) => ILayoutCheckService);
