export declare enum PluginOrder {
    /** Расширение будет вызвано в порядке регистрации. */
    Normal = 0,
    /** Расширение будет вызвано раньше чем остальные (кроме {@see System}). */
    Priority = -100,
    /** Расширение будет вызвано раньше чем остальные. */
    System = -200,
    /** Расширение будет вызвано позже чем остальные (кроме {@see System}). */
    RevertPriority = 100,
    /** Расширение будет вызвано позже чем остальные. */
    RevertSystem = 200
}
