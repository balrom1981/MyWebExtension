/** Сервис для работы с экспериментальными фичами. */
export interface IExperimentalFeatureService {
    /** Проверяет доступность экспериментальной фичи по ее ID */
    featureIsAvailable(id: string): any;
}
export declare type $ExperimentalFeature = {
    experimentalFeature: IExperimentalFeatureService;
};
export declare const $ExperimentalFeature: string | ((model?: $ExperimentalFeature) => IExperimentalFeatureService);
