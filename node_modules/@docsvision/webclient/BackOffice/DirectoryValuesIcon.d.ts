import React from 'react';
import { HtmlProps } from '@docsvision/web/core/component';

import { IRowData } from '@docsvision/web/components/table/interfaces';
export interface IDirectoryValuesIconProps extends HtmlProps.div {
    row: IRowData;
    loading: boolean;
    iconClass: string;
    onIconClick?: (arg: {
        event: React.MouseEvent;
        row: IRowData;
    }) => void;
    tooltipText?: string;
}
export declare function DirectoryValuesIcon(props: IDirectoryValuesIconProps): JSX.Element;
