/** @internal */
export declare enum FileListItemState {
    /** Added, not uploaded yet */
    New = 0,
    /** Uploading */
    Saving = 1,
    /** Uploaded to the server */
    Saved = 2,
    /** If file open in editor */
    OpenInEditor = 3
}
