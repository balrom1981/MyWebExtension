import React from 'react';
import { HtmlProps } from '@docsvision/web/core/component';

export interface IEmployeesTabFrameProps extends HtmlProps.div {
    renderHead: () => React.ReactNode;
    renderTable: () => React.ReactNode;
    renderEmployeePanel?: () => React.ReactNode;
    showEmployeePanel?: boolean;
}
export declare function EmployeesTabFrame(props: IEmployeesTabFrameProps): JSX.Element;
